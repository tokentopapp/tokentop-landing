<section class="py-24 bg-[var(--color-bg-header)] border-y border-[var(--color-border)] relative overflow-hidden" id="demo">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center max-w-3xl mx-auto mb-12 reveal">
      <h2 class="text-3xl md:text-5xl font-bold tracking-tight mb-4">
        See it in action
      </h2>
      <p class="text-lg text-[var(--color-text-muted)]">
        Watch tokentop monitor AI sessions in real-time.
      </p>
    </div>

    <!-- Video -->
    <div class="w-full max-w-5xl mx-auto reveal">
      <div class="rounded-xl overflow-hidden">
        <video 
          class="w-full h-auto block" 
          controls 
          autoplay 
          muted 
          loop 
          playsinline 
          poster="/assets/Screenshot.png"
        >
          <source src="/assets/ttop.mp4" type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>

      <p class="mt-8 text-center text-sm text-[var(--color-text-muted)]">
        Try it yourself — run <code class="text-[var(--color-primary)] font-mono bg-[var(--color-bg-card)] px-2 py-0.5 rounded text-xs">ttop demo</code> for a live preview with sample data.
      </p>
    </div>
  </div>
</section>
